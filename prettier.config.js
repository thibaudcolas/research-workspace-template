/**
 * Prettier configuration for the workspace.
 * See https://prettier.io/docs/configuration.
 * @type {import("prettier").Config}
 */
module.exports = {
  // Don't re-wrap prose in Markdown files. The documentation style guide
  // requires avoiding hard-wrapping, so lines are left as written.
  proseWrap: "preserve",
};
