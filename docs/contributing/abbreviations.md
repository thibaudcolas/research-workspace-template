_[PSF]: Python Software Foundation
_[WG]: Working Group

<!-- Abbreviations registered here become hover tooltips on every page, via the `pymdownx.snippets` `auto_append` config in `mkdocs.yml`. Replace these examples with your project's abbreviations. This file is not in `nav` — it's a snippet source. -->
